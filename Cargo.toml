[package]
name = "memo"
version = "0.1.0"
edition = "2024"
description = "Personal memory for AI agents, written in Rust"
authors = ["Christopher Cummings"]
license = "MIT"
repository = "https://git.sr.ht/~averagechris/memo"
homepage = "https://git.sr.ht/~averagechris/memo"
readme = "README.md"
categories = ["command-line-utilities"]

[[bin]]
name = "memo"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[profile.release]
lto = "fat"
codegen-units = 1
strip = "symbols"
opt-level = "s"
panic = "abort"
